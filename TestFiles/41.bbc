using System;

temp string builder = "";

PrintLine(builder);

builder = (temp builder) + "a";
PrintLine(builder);

builder = (temp builder) + "b";
PrintLine(builder);

builder = (temp builder) + "c";
PrintLine(builder);

builder = (temp builder) + "d";
PrintLine(builder);

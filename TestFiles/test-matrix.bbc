using System;
using System.Lists;

[CodeEntry]
void Start()
{
	Array<Array<int> > matrix = new Array<Array<int> >(5);

	for (int y; y < 5; y++)
	{
		matrix[y] = new Array<int>(10);
		for (int x; x < 10; x++)
		{
			matrix[y][x] = x;
		};
	};
}
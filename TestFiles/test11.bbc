using System;
using System.Lists;

{
  Array<int> list = new Array<int>(3);

  PrintLine(temp list.ToString());
  // list.Push(420);

  /*
  PrintLineD(list.ToString());
  list.Insert(1, 69);
  PrintLineD(list.ToString());
  list.Pop();
  PrintLineD(list.ToString());
  list.RemoveAt(0);
  PrintLineD(list.ToString());
  */
  
  delete list;

  /*
  Map map = new Map(10, 10);
  PrintLine(map.Get(5, 5));
  map.Set(4, 8, 333);
  PrintLine(map.Get(4, 8));
  delete map;
  */
}
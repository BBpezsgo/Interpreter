using System;

int Add(this int a, int b)
{
  return a + b;
}

{
  // Console.Log((0).Add(5).Add(2).ToString());
  // Console.Log(Time.tmnw().Split(":")[2]);
  PrintLine(Time.Now().ToTimeSpanString());
  // Main();
}

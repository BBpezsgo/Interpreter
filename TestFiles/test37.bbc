using System;
using System.Lists;

Array<int> bruhs = new Array<int>(3);
bruhs[0] = 3;
bruhs[1] = 6;
bruhs[2] = 9;

void Do(Array<int> list, func<void, int> func) {
	for (int i; i < list.Length(); i++) {
		func(list[i]);
	};
}

void PrintElement(int element) { PrintLine(element); }

Do(bruhs, PrintLine);

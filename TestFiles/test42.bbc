using System; 

int ptr = Alloc(6);

char[5] value = "12345";

for (int i = 0; i < value.Length; i++) {
	*(ptr + i) = value[i];
};
*(ptr + 5) = '\0';

void PrintString(int ptr) {
	while (*ptr) {
		Print((*ptr) as char);
		ptr++;
	};
}

PrintString(ptr);

delete ptr;
